name: observational_memory
kind: exclusive
version: 1.5.0
description: "Observational Memory — local-first shared memory across Hermes, Claude Code, Codex, OpenCode, Kimi, Grok, and Cowork."
author: "Bryan Young"
provides_tools:
  - om_context
  - om_search
  - om_remember
provides_hooks:
  - on_session_end
  - on_memory_write
pip_dependencies:
  - observational-memory>=0.9.0,<0.10
hooks:
  - on_session_end
  - on_memory_write
