name: observational_memory
kind: exclusive
version: 1.3.1
description: "Observational Memory — local-first shared memory across Hermes, Claude Code, Codex, Grok, and Cowork."
author: "Bryan Young"
provides_tools:
  - om_context
  - om_search
  - om_remember
provides_hooks:
  - on_session_end
  - on_memory_write
pip_dependencies:
  - observational-memory>=0.6.3,<0.7
hooks:
  - on_session_end
  - on_memory_write
